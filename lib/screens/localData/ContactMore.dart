import 'package:flutter/material.dart';

class ContactAndMore extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('contact and more'),
    );
  }
}
